// source-map.coffee
var func1, func2;

import {
  undef
} from '@jdeighan/base-utils/ll-utils';

// ---------------------------------------------------------------------------
func1 = (x) => {
  // return twice the argument
  return 2 * x;
};

// ---------------------------------------------------------------------------
func2 = (x) => {
  // --- return half the argument
  return x / 2;
};

console.log("DONE");

//# sourceMappingURL=source.js.map
